#ifndef VERSION_H
#define VERSION_H

// The major version of the project
#define LEVELDB_SHELL_VERSION_MAJOR @PROJECT_VERSION_MAJOR@

// The minor version of the project
#define LEVELDB_SHELL_VERSION_MINOR @PROJECT_VERSION_MINOR@

#define STRINGIFY(x) #x
#define TO_STRING(x) STRINGIFY(x)

#define LEVELDB_SHELL_VERSION_STRING TO_STRING(LEVELDB_SHELL_VERSION_MAJOR) "." TO_STRING(LEVELDB_SHELL_VERSION_MINOR)

#endif // VERSION_H
